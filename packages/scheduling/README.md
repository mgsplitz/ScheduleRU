# Scheduling

Semester schedule preference normalization, accumulation, filtering, ranking,
and tradeoff selection live here. The package is deterministic and owns no
model calls, HTTP handlers, DOM, or catalog content.
