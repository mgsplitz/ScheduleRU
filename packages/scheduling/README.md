# Scheduling

Semester schedule preference normalization, accumulation, filtering, ranking,
and tradeoff selection live here. Meeting-time normalization, conflict
detection, blocker reporting, and bounded section-permutation generation also
live in the pure semester-schedule model. The package is deterministic and
owns no model calls, HTTP handlers, DOM, campus data, or catalog content.
